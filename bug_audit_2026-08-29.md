# stock_dashboard 全面 Bug 稽核 (2026-08-29)

## 怎麼查的

把整包 114 個 .py + 3 個 workflow 複製到雲端,分六個切片平行細讀(推播管道 / 排程大腦 / alert 模組 / 資料與指標層 / 選股與追蹤 / dashboard),
再由我逐條**親自驗證**——能跑的就跑合成資料重現,不能跑的就對照上下游程式碼確認。

**標記說明**:
- ✅ **已驗證** = 我自己重現或用程式碼對照確認過
- ⚠️ **待確認** = 子代理提出、邏輯上成立,但我沒有獨立重現(通常是需要真實 API 資料才能證實)

也**排除了一個誤報**:有個切片回報 `notifier.fmt_monitor_alerts()` 沒有 `return`。我用 AST 掃全 repo
所有「宣告回傳型別但沒有任何有值 return」的函式,結果是 **0 個**——該函式在第 952 行,有 3 個 return。不是 bug。

---

# 🔴 P0 — 功能完全失效(不是偶爾出錯,是從來沒有work過)

## P0-1 ✅ 你的自選股,8 個 alert 模組從來沒有掃描過

**位置**: `volume_breakout_alert.py:84`、`chip_anomaly_alert.py:119`、`news_event_alert.py:193`、
`breakout_consolidation_alert.py:172`、`analyst_insider_alert.py:147`、`short_candidates.py:129`、
`strong_stock_alert.py:152 / 339`、`index_alerts.py:490 / 604`

`watchlist_store.load_watchlist()` 回傳的是 **dict 陣列**:

```python
def load_watchlist() -> List[Dict]:
    return [_normalize_item(d) for d in data][:MAX_WATCHLIST]
# _normalize_item → {"stock_id":..., "name":..., "market":..., "entry_price":..., "added_date":...}
```

但上面 8 處全部把元素當成「代號字串」用,兩種壞法都是靜默的:

```python
# 壞法 A — volume_breakout / chip_anomaly / news_event / breakout_consolidation / analyst_insider
for sid in wl:
    s = str(sid).strip().upper()          # → "{'STOCK_ID': '6669', 'NAME': '緯穎'}"
    universe.append((s, "TW" if s.isdigit() else "US"))   # .isdigit() = False → 當成美股代號丟給 yfinance

# 壞法 B — short_candidates / strong_stock_alert / index_alerts
universe = list(dict.fromkeys(universe + wl))   # TypeError: unhashable type: 'dict'
except Exception: pass                           # ← 整份自選股靜靜蒸發, log 一個字都沒有
```

**實測**:
```
list(dict.fromkeys(['2330'] + [{'stock_id':'6669'}]))  → TypeError: unhashable type: 'dict'
str({'stock_id':'6669','name':'緯穎'}).strip().upper() → "{'STOCK_ID': '6669', 'NAME': '緯穎'}"
   .isdigit() → False
```

**後果**: 你把 6669 加進自選股之後,量爆突破、籌碼異常、新聞事件、盤整突破、分析師/內部人、
弱勢股、強勢股、大盤警報 —— **全都沒有真的掃到它**。只有剛好落在各模組預設 universe 裡的標的會被掃到。
`app.py` 那邊是照 dict 正確寫的,所以 dashboard 看起來完全正常,只有推播端是瞎的。

**這是整份清單裡最嚴重的一個。**

---

## P0-2 ✅ `_run_with_timeout` 的 60 秒上限完全無效,而且 log 會騙你

**位置**: `scripts/market_open_alert.py:89`

```python
with _cf.ThreadPoolExecutor(max_workers=1) as ex:     # ← __exit__ 會 shutdown(wait=True)
    fut = ex.submit(fn)
    try:
        return fut.result(timeout=timeout_sec)
    except _cf.TimeoutError:
        print(f"[monitor timeout] {name} > {timeout_sec}s, skip")
        return default                                 # ← 這行之後才卡在 __exit__
```

`with` 區塊離開時 `ThreadPoolExecutor.__exit__` 會呼叫 `shutdown(wait=True)`,而執行中的 future 無法 cancel,
所以 **`return` 之後函式仍會阻塞到 `fn()` 自然跑完**。60 秒的界從來沒有生效過,只有那行 log 會準時印出來。

**實測**(timeout 設 2 秒、工作要跑 8 秒):
```
[monitor timeout] slow > 2s, skip  (t=2.0s)      ← log 準時
函式真正 return 的時間: t=8.0s (期望 ~2s)         ← 實際卡滿 8 秒
```

**後果**: monitor tick 裡任何一個 check 被 yfinance 限流卡住(常見),log 會顯示「已 timeout 跳過」,
但 process 實際卡在那裡。配上 workflow 的 `timeout-minutes: 20`,排在後面的
**持倉停損警報 (Tier 0)**、watchlist_triggers、news_event、volume_breakout、chip_anomaly 全部沒跑就被砍掉。
你當天收不到持倉風險警報,而 log 顯示一切正常。

> 同一份程式碼在 `news_sources.fetch_external_signals_bounded()` 是寫對的(明確 `ex.shutdown(wait=False)`
> 並在註解裡解釋要避開這個阻塞),可見是漏改了這一處。

---

## P0-3 ✅ 籌碼異常警報 (chip_anomaly) 從來沒有推播過任何一則

**位置**: `chip_anomaly_alert.py:66`,門檻在 79 / 90 行

```python
if not row.empty and "資本額" in row.columns:      # ← 這個欄位不存在
    cap = float(row.iloc[0].get("資本額", 0))
    shares_lots = cap / 10000 if cap > 0 else 0
else:
    shares_lots = 0
...
pct_of_outstanding = abs(cum_5d) / shares_lots * 100 if shares_lots > 0 else 0
if is_streak_buy and pct_of_outstanding > 1.0:     # ← 恆為 False
```

`data_sources.get_taiwan_stock_info()` 回的是 FinMind `TaiwanStockInfo` 原始欄位,`_ensure_cols()` 只保證
`stock_id` / `stock_name` / `type` 三欄,**永遠不會有中文的「資本額」**。
於是 `shares_lots` 恆為 0 → `pct_of_outstanding` 恆為 0 → 兩個 `> 1.0` 的門檻永遠不成立。

**後果**: 整個 Tier 2 籌碼模組(含 `notifier.fmt_chip_anomaly_alerts`、去重、rollback 全套機制)
從未推播過任何一則,而且外觀跟「今天沒有籌碼異常」完全無法區分。

---

## P0-4 ✅ 外資台指期未平倉抓到的是隨機合約,不是台指期

**位置**: `institutional_positioning.py:128`(篩選)、`:142`(賦值)

```python
rows = [r for r in rows if r.get("futures_id") == "TX"
        or r.get("contract_type") == "TXF"
        or r.get("name") in ("Foreign_Investor", "Investment_Trust", "Dealer")]   # ← 第三個條件對每一列都成立
...
    by_date[d]["Foreign_Investor"] = net_oi     # ← 用 = 不是 +=, 同一天多個合約互相覆蓋
```

第三個 `or` 條件讓「只留 TX」的意圖完全失效(MTX 小台、TE 電子期、TF 金融期的列同樣有這些 name),
接著用 `=` 覆寫,最後留下的是 FinMind 回傳順序中的最後一列。

**後果**: 同一天 TX 淨 +18000 口、MTX +200 口、TE -400 口 → 最終 `foreign_oi` = **-400**(電子期),
正確值應是 +18000。推播顯示「外資台指期淨 -400 口 ⚪ 中性」,籌碼面標籤從「強烈偏多」翻成「中性」。

---

## P0-5 ✅ 美股個股對台股族群的連動加分,21 個題材全部匹配不到

**位置**: `cross_market_bridge.py:165`

```python
matched_stocks = [us for us, tw_list in US_THEME_STOCK_TO_TW.items()
                  if any(theme.lower() in tw.lower() or tw.lower() in theme.lower()
                          for tw in tw_list)]
```

`theme` 是族群名(`"重電族群"`、`"AI 伺服器"`),`tw_list` 的元素是個股字串(`"中興電 (1513)"`、`"華城 (1519)"`)。
兩者互相都不是對方的子字串。

**實測**: 遍歷全部 21 個 theme,`matched_stocks` **一律是空 list**。
所以 NVDA 單日暴漲 8% 對台股 AI 伺服器族群的加分是 **0**,模組 docstring 自述「個股級別,比 sector ETF 更精準」
的那一半功能從未生效。

---

## P0-6 ✅ 大盤點數警報的階梯與冷卻,兩個 state 欄位只讀不寫

**位置**: `index_alerts.py:1712-1763`

```python
last_bucket = sym_state.get("last_bucket", 0)
if bucket != last_bucket and abs(bucket) >= threshold:
    ...
    last_at = sym_state.get("last_alert_at")      # 30 分鐘冷卻
    ...
    sym_state["last_alert_diff"] = diff
    sym_state["last_alert_price"] = current
    sym_state["alerts_today_count"] = alerts_count + 1
    # ← 沒有寫 last_bucket, 也沒有寫 last_alert_at
```

grep 全檔:`last_bucket` 只在 1704 行(跨日重置成 0)被寫過,`last_alert_at` 在這個函式裡只有 1709 行(重置成 None)。
於是 `bucket != last_bucket` 在 `last_bucket` 恆為 0 的情況下,只要 `abs(bucket) >= threshold` 就永遠成立,
「每 X 點一格」的階梯完全失效;`last_at` 永遠是 None,30 分鐘冷卻那段永遠不會執行。

**後果**: 唯一還有效的節流只剩「每日 4 則上限」。指數在同一格內盤整時會連吐 4 則相同警報才被日上限擋住。
(目前 monitor 已改不呼叫此函式,由 dashboard 手動按鈕觸發;一旦重新接回排程即為每 tick 洗版。)

---

# 🟠 P1 — 會產生錯誤數字或錯誤排序

## P1-1 ✅ 「潛伏題材股」沒有下界,暴跌爆量股會排在推薦第一名

**位置**: `sector_pulse.py:384`

```python
cond = (
    (all_df["今日%"].fillna(0) <= 2)      # 只有上界
    & (all_df["量比"].fillna(0) >= 1.5)
    & (all_df["5日%"].fillna(0) <= 5)     # 只有上界
)
stealth = all_df[cond].sort_values("量比", ascending=False).head(15)
```

docstring 明訂條件是「個股當天漲幅 **>0**(開始發動)」「close > 5 日均線」,但實作只有三個上界,
沒有任何下界,也沒有 MA5 檢查。任何崩跌中的爆量股都符合,而且因為按**量比降冪**排序,
跌最兇、量最大的(通常正是恐慌殺盤)會排在最前面。

標題是「潛伏題材股(族群熱、本身還沒大漲、有量能跡象)」,實際第一名可能是當日重挫 9.5%、五日跌 22% 的股票。

---

## P1-2 ✅ 「大戶偷偷進場」的分數門檻恆真,而且同樣沒有下界

**位置**: `smart_money_stealth.py:164 / 181 / 215`

```python
cond_today = today_pct <= 2.0          # 無下界
cond_theme = theme_avg_pct >= 2.0      # scan 端只送 theme_avg >= 2.0 的成分股進來 → 恆 True (+3)
...
if score < 6:
    return None                        # 兩個必要 gate 3+2 分 + 恆真的 3 分 = 8 > 6, 門檻等於不存在
```

註解宣稱「加分: 至少 1 個籌碼 或 (20MA + 整理)」完全沒被強制。熱門題材裡某檔今日 **-9.0%**、
量比 1.8x(放量殺盤出貨)一樣會進「🕵️ 大戶偷偷進場 Top 5」,理由欄還會印
「📉 今日僅 -9.00% (≤+2.0%, 還沒被注意)」。

附帶:`main_broker_buy` 初始化成 False 後**沒有任何賦值路徑**,「主力券商 buy_top」是永遠印不出來的死分支。

---

## P1-3 ✅ Weinstein Stage 分析的「頂部」判斷被「上升期」遮蔽,結論剛好反過來

**位置**: `stage_analysis.py:70-88`

```python
if slope_pct > 1.0 and price_above:
    out["stage"] = 2                                          # 上升期 (持有/買進)
elif slope_pct < -1.0 and not price_above:
    out["stage"] = 4
elif slope_pct > 0.5 and price_above and price_dist_pct > 15:
    out["stage"] = 3                                          # 頂部 (準備出場) ← 只剩 0.5 < slope <= 1.0 這條窄縫
```

真正漲多的股票,30 週均線斜率一定 > 1%,會先被 Stage 2 接走 → **拿不到「準備出場」標籤**。
反而是「長期橫盤後剛噴出」(MA 還沒跟上、斜率仍小)的股票會被誤標成頂部。
這個結果直接餵給 `ensemble_voter` 的 Weinstein 票(Stage 2→BUY、Stage 3→HOLD)。

---

## P1-4 ✅ 「今日避開」清單讀了三個不存在的欄位

**位置**: `avoid_signals.py:57 / 75 / 110`

```python
ma20_diff = price.get("距 20MA %")     # chip_analyzer 實際 key: 沒有這個
five_d    = price.get("5日%")          # 實際是 "5d漲跌%"
"current": price.get("收盤"),          # 實際是 "close"
```

`chip_analyzer` 寫入的 price key 只有 `close` / `今日%` / `5d漲跌%` / `20d漲跌%` / `量比` / `今日量` / `20日均量`。
這三個字串在整個 repo **只出現在 avoid_signals.py 裡**,沒有任何地方寫入。

**後果**: docstring 宣稱的「跌破月線 +2 分」與「近 5 日連跌 +1 分」兩條永遠拿 0,滿分從 11 掉到 8;
每筆 pick 的 `current` 恆為 `None` → `market_open_picks.py:576` 的 `if sid and cur:` 恆為 False →
**`avoid_pick` 從來沒有任何一筆被寫進 signal_tracker**,而 `fmt_accuracy_block()` 預設清單裡就有它,
導致「避開訊號→3日跌」的勝率永遠空白。

---

## P1-5 ✅ ensemble_voter 兩個 key 對不上

**位置**: `ensemble_voter.py:46`、`:237`、`:299`

```python
STRATEGY_WEIGHTS = {"Minervini":1.5, "CANSLIM":1.2, "Weinstein":1.3,
                    "Buffett":1.0, "Speculation":0.7, "entry_evaluator":1.5}
...
strat_name = (d.get("strategy") or "").split()[0]
w = STRATEGY_WEIGHTS.get(strat_name, 1.0)
```

六個策略的 `strategy` 字串分別是 `"Minervini SEPA+VCP"` ✓、`"Weinstein Stage"` ✓、`"Buffett Quality"` ✓、
`"CANSLIM"` ✓、`"Speculation"` ✓、以及 **`"綜合評估"`** ✗ —— 最後這個 split 出來是 `"綜合評估"`,
查不到表(表上的 key 是 `"entry_evaluator"`),落到 default 1.0,而不是設定的 1.5。權重最高的策略被降權。

同一支的第二個 key 錯誤:

```python
hot_themes = themes.get("themes", [])     # theme_analyzer 回的 key 是 "narrative_tags"
```

→ `_vote_speculation` 恆走 else 分支,**永遠投 HOLD**。RKLB 同時命中 Space + Defense + AI 三個題材、
`total_score` 80/100,投票結果仍是「無熱門題材曝險」。六策略投票中永遠不可能有 Speculation 的 BUY 票,
`STRONG_BUY` 需要 `buy_n >= 3` 因此更難達成。

---

## P1-6 ⚠️ 一整組「股 vs 張」差 1000 倍的單位錯誤

FinMind 法人買賣超的 `buy`/`sell` 是**股**,`fetch_shares_outstanding` 回的是**張**。以下各處混用:

| 位置 | 症狀 |
|------|------|
| `tw_screener.py:340` | 投本比放大 1000 倍。投信買 2000 張的股票算出 7.71%,遠超 1.5% 門檻(正確值 0.0077%)→ 任何投信買個幾百張都命中 |
| `chip_price_divergence.py:122` 起 9 處 | 門檻 1000/1500/2000「張」實際是股。外資買 **2 張** 就觸發「三大法人共識買 + 放量上漲 / strong_bullish / 可考慮加碼」 |
| `institutional_positioning.py:74` | 股數 `/1e8` 當「億元」。外資淨買 8 萬張顯示成「+0.80 億」,而 `bias_score` 的 ±2 分支因為門檻是 ±50 億 → **永遠不會觸發** |
| `avoid_signals.py:43` | 外資賣超門檻 -3000/-2000「張」實際是 3 張/2 張 |
| `emerging_themes.py:123` | docstring 寫「投信 5d > 1000 張」,實際是 1000 股(= 1 張) |
| `tw_screener.py:700` | `min_avg_volume = 500`(註解寫「張」)比的是股 → 5 日均量 0.6 張的極冷門股照樣通過「排除冷門股」過濾 |

這組我沒有逐一跑數字驗證(需要真實 FinMind 資料),但欄位單位在 repo 內部有明確自證:
`chip_anomaly_alert.py:41` 註明「buy, sell (股)」並自己 `/1000`,`entry_evaluator.py:634` 同樣 `/1000.0`。

---

## P1-7 ⚠️ AI 信心重排把兩種尺度混在同一個 sort key

**位置**: `ai_confidence_scorer.py:186-195`

```python
base = score if score is not None else (entry_score if entry_score is not None else 5)
mult = 10 if entry_score is not None else 1
return base * mult / 10          # fallback 分支
scored_picks.sort(key=_key, reverse=True)
```

有 AI 分數的走 `ai_adjusted_score`(0-10 尺度),沒被 AI 評到的走 fallback,對 `entry_score`(0-100)
等於原封不動回傳。兩條分支量綱完全不同卻共用一個 sort。

Gemini 回應被 `max_output_tokens` 截斷、只回了 20 檔中的 12 檔(實務常見)時,
**完全沒被 AI 評估的標的會被推到「今日 Top 可行動」第一名**,而 AI 判定最好的那檔掉到第二。

---

# 🟡 P2 — 重複推播 / 時間錯亂

## P2-1 ✅ 「加權指數盤中大漲」的漲幅是跨隔夜的,不是今日開盤

**位置**: `strong_stock_alert.py:50`

```python
today_bars = twii.tail(50)                       # 沒有依日期過濾
open_price = float(today_bars.iloc[0]["Open"])
twii_pct = ((current_price - open_price) / open_price) * 100
if twii_pct >= 1.5:
```

抓的是 `period="2d", interval="5m"`,台股一個交易日只有 54 根 5m K,所以盤中任何時刻
`tail(50)` 都會跨到昨天,`twii_pct` 實際是「距今 4 小時 10 分前的漲幅」,含整段隔夜跳空。

同一檔的 `_stock_strength_metrics()` 有正確做 `df["_d"] == today` 過濾,只有這裡漏了。

**後果**: 昨日尾盤弱、今日只漲 0.4% 也可能算出 +1.75% → 誤推「🔥 加權指數盤中大漲」並連帶送出整批強勢股;
反之真正的大漲會被算小而漏推。

## P2-2 ⚠️ 三個 alert 用 UTC 日界去重,美股標的隔天早上會原封不動重推

| 位置 | 問題 |
|------|------|
| `volume_breakout_alert.py:76` | `vb_state["date"]` 用 `dt.date.today()`(UTC)。UTC 日界 = 台北 08:00,亞股 monitor tick 時去重表已清空,但美股日線最後一根還是昨夜那根 → 條件全數成立 → Tier 1 響鈴重推。姊妹模組 `breakout_consolidation_alert.py` 已改用美東日期,這支沒跟上 |
| `news_event_alert.py:325` | 去重表每天清空,但新聞來源是 `days_back=2` 回看窗,且沒有任何時效過濾(對照 `trump_policy_alert` 有 `TRUMP_MAX_AGE_HR = 12`)。同一份 8-K 隔天會再以「🚨🚨 急報」響鈴推一次 |
| `holdings_intraday_alert.py:106` | 台股時段用美股「昨日」K 棒判斷(`today = df["_d"].max()` 沒有跟系統日期比對是否過期)。NVDA 週二美股 -4% 會在週三台北早上再推一次「今日大跌 -4.00%」,而當時美股根本沒開盤 |

## P2-3 ⚠️ 週末的 8-K / 重大新聞掃描,一則都掃不到

**位置**: `scripts/market_open_alert.py:1347` 擋住 `:1664`

yml 特地排了 `0 2,6,10,18,22 * * 0,6`(註解寫明「週末新聞掃描」,約 48 runs/月),router 導到 `monitor`。
但 monitor handler 第一件事是「所有市場都不在 session 就 `return 0`」,而週末必定不在 session。
真正負責掃 8-K 的 `news_event_alert.check_news_events()` 在第 1664 行,遠在 early-exit 之後。

諷刺的是該模組自己的閘門寫著「週末: 全天放行(新聞/8-K filings 24x7 都會發)」——它本來就是設計來週末跑的。

## P2-4 ⚠️ 美股的均線/KD/MACD 條件觸發永遠不會被評估

**位置**: `watchlist_triggers.py:135`

cross 類觸發要求 `require_closed=True`,而 `_is_market_closed_now("US")` 要 UTC ≥ 21 才算收盤。
但 `check_triggers()` 只在 monitor 模式被呼叫,monitor 的 session early-exit 在 UTC 21:00 後必定 return,
workflow 的美股 monitor cron 最晚也只到 `15 20 * * 1-5`。**兩個時窗完全不重疊。**

你設「NVDA 站上 20 日均線通知我」→ 永遠 armed、永遠不觸發,也不會出現在 15:00 盤後彙總。

## P2-5 ⚠️ `morning_recap` 被歸到美股行事曆,週日那條 cron 必被跳過

**位置**: `scripts/market_open_alert.py:338`

cron 是 `32 23 * * 0-5`(週日 UTC 23:32 = 週一 07:32 TPE),但假日檢查把 `morning_recap` 當美股,
週日的 ET 日期就是週日 → `weekday() >= 5` → `return 0`。整條週日排程永遠到不了 handler。
(目前該 handler 已 no-op,所以現在不會少推,但一旦重新啟用就會每週一靜默消失。)

## P2-6 ⚠️ 長訊息「先分段、後檢查每日上限」→ 上限滿時你會收到沒頭的殘篇

**位置**: `notifier.py:242` vs `:275`

分段在 cap 檢查之前,且只有第 1 段帶 `category`。當日 cap 已滿時,第 1 段被擋(不送),
第 2 段之後 `category=None` 完全繞過 cap 照送。cap 的語意是「這封不要送」,實際變成「這封送一半」。

## P2-7 ⚠️ 送出後回寫舊快照,可能把剛寫入的 cap 計數與去重標記抹掉

**位置**: `watchlist_store.py:128`,觸發點 `strong_stock_alert.py:283 / 588`

慣例是「開頭 load state → 做慢工 → send_message → 最後 save 那份舊快照」。但 `send_message()` 內部
自己也會寫 state(`push_cap.mark_consumed` +1、`record_push` 追加),最後那次 save 帶的是 send **之前**的快照,
`merged.update(state)` 會用舊值覆蓋剛寫進去的新值。批次模式更糟,完全沒有 merge。

---

# ⚪ P3 — 小問題

| # | 位置 | 問題 | 驗證 |
|---|------|------|------|
| P3-1 | `signal_tracker.py:229` | `validated[-keep_validated:]` 在 `keep_validated == 0` 時等於 `validated[0:]` → **保留全部**而非 0 筆。實測 `[1,2,3][-0:]` → `[1,2,3]` | ✅ |
| P3-2 | `portfolio_risk.py:240` | `stocks_w` 完整算出來後被丟棄,`return` 沒帶 `"stocks"` key(docstring 和空持倉分支都宣稱有)→ dashboard「🏆 個股權重 Top 5」永遠不顯示 | ✅ |
| P3-3 | `cross_market_bridge.py:209` | `"6669"` 在 dict literal 中重複定義,緯穎從「AI 伺服器」被靜默覆蓋成「ABF 載板」。實測 `.get("6669")` → `'ABF 載板'` | ✅ |
| P3-4 | `econ_calendar.py:96` | 非農公布時間寫死「台北 20:30」。EST(11 月–3 月)實際是 **21:30**,一年有 4-5 次會早一小時守盤 | ✅ |
| P3-5 | `short_interest_alert.py:43 / 115` | 用 snake_case 取 FinMind 欄位(`balance` / `margin_purchase_today_balance`),但該 dataset 回的是 CamelCase(repo 其他地方一律用 `MarginPurchaseTodayBalance`)→ 每天固定顯示「借券餘額 0 億」,券資比整段消失 | ✅ |
| P3-6 | `indicators.py:73` | `rsi()` docstring 寫「短序列回 NaN」,實作是 `out.fillna(50)` → 暖身期與資料不足都回 50,跟真正中性無法區分 | ⚠️ |
| P3-7 | `data_sources.py:508` | `check_daily_freshness` 用日曆日相減,週一拿到正常的週五收盤會被判「資料過期」 | ⚠️ |
| P3-8 | `chip_advanced.py:110` | 千張大戶的正則 `contains("1000|1,000|...")` 是子字串比對,`"1,000-5,000"`(1~5 張,散戶級距)也會被算進去並加總 | ⚠️ |
| P3-9 | `chip_price_divergence.py:76` | `fetch_chip_data(days=10)` 的 10 是日曆日 → 只有 6-7 個交易日,但輸出 key 仍叫「20日均量」,`量比` 門檻被錯誤基準餵食 | ⚠️ |
| P3-10 | `event_sources.py:156` | 台股重大訊息 `rows[:30]` 沒排序,FinMind 回傳是日期遞增 → 取到的是**最舊**的 30 筆 | ⚠️ |
| P3-11 | `indicators.py:437` | `is_52w_high_breakout` 只要 60 根 K 就輸出 key 叫 `52w_high` 的結果,上市 3 個月的新股會被判「突破 52 週新高」 | ⚠️ |
| P3-12 | `actionable_picks.py:193` | 讀 `get_tw_open_picks()` 的 `potential_picks` key,但該函式從不回傳這個 key → 整個「來源 1」是死路,卻仍付出 30-60 秒與 Gemini quota | ⚠️ |
| P3-13 | `pick_review.py:171` | 「近 30 日累計勝率」呼叫 `accuracy_summary(None)` 不分方向,把放空訊號猜對也算進標題寫著「隔日**上漲**比例」的分母 | ⚠️ |
| P3-14 | `scripts/run_backtest.py:36` | `import backtest_upside` — 這個檔案不存在。獨立 CLI,不影響推播,但執行必定 ImportError | ✅ |

---

# ✅ 這次已經修掉的

## 我自己上一輪埋的 bug — 備援 cron 被自己的去重廢掉

上一輪加了備援 cron(晨報 08:02/08:13/08:27、盤後 15:03/15:19)並把 dedup 窗拉到 300 分。
但 `claim_slot()` 是在 `main()` **一進來**就寫時間戳的,跟「這次到底有沒有推出去」完全脫鉤 ——
15:03 那班只要在抓資料階段掛掉,slot 已被佔,15:19 的備援會被自己的 dedup 擋掉,當天整封消失,
而 log 只印「視為重複」看起來像正常去重。**等於備援 cron 白加了。**

改成 `push_cap` 早就用的模式(唯讀檢查 + 送出成功才登記):
- `notifier.SEND_OK_COUNT` 記錄本 process 成功送出幾封
- 守衛處只做 `was_claimed_recently()` 唯讀檢查
- 用 `atexit` 在 process 結束時登記,且**只有真的推出去了才登記**

**三個獨立 process 實測**:
```
Run 1 (15:03 主班, Telegram 掛掉)
  [limit_up_precursor] sent ok=False
  [push_dedup] 本次一封都沒推出去 → 不登記, 備援 cron 仍可補推
Run 2 (15:19 備援)
  >>> TELEGRAM 收到一封
  [push_dedup] 本次有成功推播 → 登記 claim
Run 3 (又一班遲到的 cron)
  [push_dedup] 稍早已成功推播過, 本次跳過。
```

---

# 建議的處理順序

1. **P0-1(自選股)** — 影響 8 個模組,而且是你每天都在用的功能。改法統一:
   `wl = [str(x.get("stock_id","")).strip().upper() for x in (load_watchlist() or []) if x.get("stock_id")]`
2. **P0-2(timeout 無效)** — 一行改動(`with` 改成手動 `shutdown(wait=False)`),但它保護的是持倉停損警報
3. **P0-3 / P0-4 / P0-5** — 三個「從來沒work過」的功能,各自獨立、改動不大
4. **P1-6(股 vs 張)** — 需要對 FinMind 每個 dataset 的單位做一次盤點,改完要重新校準門檻
5. 其餘照 P1 → P2 → P3

要我從哪一項開始?我建議 P0-1 和 P0-2 一起做,那兩個的修法都很單純,但影響面最大。

> 老話一句:這些全部都還沒部署。`.git` 仍然不存在,GitHub 上跑的是舊版。
