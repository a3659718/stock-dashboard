# 隱藏 Bug 稽核與修正報告 (2026-09-07)

## 怎麼查的

延續 8/24、8/29 兩輪稽核。這次分三個切片平行細讀 —— 推播管道與排程大腦 / 8-31 之後改動過的 45 支檔案 / 資料層與指標層與選股層 —— 再由我逐條**親自驗證**：能跑的就在你機器上跑合成資料重現，不能跑的就對照上下游程式碼確認。

**重點之一是複核 8/29 那 15 項「✅ 已改」是不是真的正確套用** —— 結果 14 項正確、1 項修法無效（見下面 A-2）。另外找到 4 個「這輪改動漏掉的呼叫點 / 只改一半」。

修改前已備份到 `_backup_2026-09-07/`（12 支原始檔）。全專案 `compileall` 通過，18 個關鍵模組 import 全綠。

---

# 第一段：已經幫你改好（12 項）

## 🔴 A-1 量爆突破的去重讀寫用不同日界 → 同一檔每 30 分鐘重推，還會燒光當日推播額度

**位置**: `volume_breakout_alert.py:116`

掃描端 `check_volume_breakout()` 讀的是 `_us_today_str()`（美東日期），登記端 `mark_alerts_sent()` 寫的是 `dt.date.today()`（容器 UTC 日期）。台股 session 的 monitor cron 是 `8,38 1,2,3,4 * * 1-5`，UTC 01:08–03:38 這 6 個 tick 正好落在「美東還是昨天」的窗口，兩者**必定差一天**。

後果：每個 tick 掃描端都判定「換日」→ `vb_state.clear()` → `alerted` 清空 → 觸發條件（今日 +3%、量比 3、突破 60 日高）是日線算的，一旦成立整天都成立 → **同一檔在 09:08 / 09:38 / 10:08 … 被重複推播**。而且 `category="volume_breakout"` 會把 `push_cap` 每日共用的 6 封次要額度全燒在同一檔上，當天的 news_event / chip_anomaly / 盤整突破全被擋掉。反向也錯：01:08 讀到的 `alerted` 其實是昨天美股盤的清單，昨天推過的今天會被靜默吞掉。

隔壁的 `breakout_consolidation_alert.py:160` 是對的，還特地寫了註解「跟 check 用同一個日界」—— 這支只抄了一半。

```python
# ❌ 改之前
today_str = dt.date.today().strftime("%Y-%m-%d")
# ✅ 改之後
today_str = _us_today_str()
```

**驗證**：跑過日期換算，UTC 2026-09-08 01:08 → scan=`2026-09-07`、mark=`2026-09-08`，MATCH=False；04:08 之後才會一致。

---

## 🔴 A-2 8/29 的 P2-1 修法完全沒生效 —「大盤盤中大漲」還是在用跨隔夜的 `tail(50)`

**位置**: `strong_stock_alert.py:54-59`

上一輪的修法是：

```python
_idx = twii.index
_dates = _idx.date if hasattr(_idx, "date") else None
today_bars = twii[_dates == _dates[-1]] if _dates is not None else twii.tail(50)
```

但 `data_sources.fetch_yf_history()` 內部第 442 行已經做了 `df = df.reset_index()`，回來的是 **RangeIndex**，時間在 `Datetime` 欄位裡。`hasattr(RangeIndex, "date")` 是 `False` → 每次都掉進 `twii.tail(50)` fallback，**原本的 bug 100% 還在**。

台股一天只有 54 根 5m K，`period="2d"` 抓回約 108 根，盤中任何時刻 `tail(50)` 都會跨到昨天下午 → `twii_pct` 實際是「距今約 4 小時前的漲幅」，含整段隔夜跳空。這條 `twii_pct >= 1.5` 是 `check_market_surge()` 的主觸發，錯了會直接造成大盤警報誤報／漏報，還會連帶影響 `market_open_alert.py:1565` 那個「已推大盤大漲 → 跳過常態強勢股」的分支。

改成跟同檔 `_stock_strength_metrics()` 一樣的欄位判定（那支本來就是對的）：

```python
import pandas as _pd
_dcol = "Datetime" if "Datetime" in twii.columns else twii.columns[0]
_d = _pd.to_datetime(twii[_dcol]).dt.date
today_bars = twii[_d == _d.iloc[-1]]
if len(today_bars) < 2:
    today_bars = twii.tail(50)
```

**驗證**：pandas 2.3.3 實測 `reset_index()` 後 `hasattr(df.index,'date')` = False。合成 108 根 K（昨天 54 + 今天 20）：新寫法算出今日 **+2.42%**（不觸發），舊 `tail(50)` 算出 **+6.50%**（會誤報「🔥 加權指數盤中大漲」）。

---

## 🔴 A-3 8/29 的 P0-1 漏掉第 9 個呼叫點 → 盤後籌碼-價量分析永遠不含自選股，而且沒有任何 log

**位置**: `scripts/market_open_alert.py:1016`

上一輪把 8 個 `load_watchlist()` 誤用點改成 `load_watchlist_ids()`，但漏了這一個：

```python
wl = _ws.load_watchlist() or []            # 回的是 dict 陣列
cpd_stocks = list(dict.fromkeys(cpd_stocks + wl))   # TypeError: unhashable type: 'dict'
except Exception:
    pass                                    # ← 沒有一個字的 log
```

結果 `cpd_stocks` 只剩 upside_screener 的前 20 檔，`tw_close` 那封「盤後籌碼-價量 12 模式」從來沒提到過你的自選股。改成 `load_watchlist_ids("TW")`，並把 `except: pass` 換成會印出原因的 log。

**驗證**：在你機器上重現 `list(dict.fromkeys(["2330"] + [{"stock_id":"6669"}]))` → `TypeError: unhashable type: 'dict'`。

---

## 🟠 A-4 投信「5 日累計買超 ≥ 100 張」拿股數比張數 → 這條篩選器等於恆真

**位置**: `tw_screener.py:319-321`

FinMind 的 `buy`/`sell` 單位是**股**，`five_day_acc_lots = 100` 的門檻是**張**（1 張 = 1000 股）。同一支檔案的 `screen_invtrust_capital_ratio` 已經正確寫了 `cum / 1000.0`，只有這條沒跟著改（8/29 清單第二段只列了 `:340` 和 `:296`，沒列到這裡）。

投信 5 日淨買 **105 股（0.105 張）** 也會命中「≥ 100 張」→ 只要投信淨買不是負的就入選，跟隔壁要「真的買到 1.5% 股本」的條件嚴格度差三個數量級。

**驗證**（改後實跑）：

```
投信5日淨買: 2330=110000股(110張)  2454=105股(0.105張)  6669=300000股(300張)
stock_id  5d_cum_net
    2330         110
    6669         300      ← 2454 已正確排除, 且數字現在真的是「張」
```

---

## 🟠 A-5 推播訊息上的「投本比%」被放大 1000 倍

**位置**: `tw_screener.py:695-707`（`acc5_map` / `today_map`）

`投信5日(張)` / `投信今日(張)` 兩個欄位名寫「張」，裝的卻是「股」；而 `_cap_ratio` 的分母 `shares_map` 是「張」。投信 5 日淨買 300 張、流通 200,000 張的個股：

- 門檻端（已修好那半邊）算 `0.15%` → 不觸發 ✅
- 顯示端算 `150%` → **Telegram 印「投本比 150%」** ❌

同一支檔案裡兩個「投本比」相差 1000 倍。這個欄位會直接進 `notifier.py:394 / 3301`、`app.py:1782` 的表格、`tracker.py:85` 的績效紀錄。改成在建 map 的時候就 `/1000` 換算成張，欄名、投本比、下游顯示一次全對齊。

---

## 🟠 A-6 EPS 年增率在「去年同季虧損」時符號整個反過來

**位置**: `stock_deep_analyzer.py:607-611`

只擋了「轉虧為盈」「由盈轉虧」兩種號誌反轉，**負→負完全沒擋**。分母是負數時比值方向顛倒：

| 情境 | 舊算法 | 舊標籤 | 新算法 |
|---|---|---|---|
| 去年 -2.0 → 今年 -4.0（虧損擴大一倍） | **+100%** | **高速成長** ❌ | -100% |
| 去年 -2.0 → 今年 -1.0（虧損收斂一半） | -50% | 明顯衰退 ❌ | +50% |
| 去年 -2.0 → 今年 -0.2（接近損平） | -90% | 明顯衰退 ❌ | +90% |

而且這個值會直接餵進 `entry_evaluator.py:458`：`if eyoy >= 30: score += 5; reasons.append("✅ EPS YoY +100.0% 高成長")` —— 虧損翻倍的公司拿到 +5 分跟一句「高成長」。改成分母取絕對值。

---

## 🟠 A-7 美股題材關鍵字用裸子字串比對，`"AI"` 命中 `retail`、`"EV"` 命中 `revenue`

**位置**: `us_screener.py:117-126`

全部轉小寫後做 `in` 比對，兩個字母的 `AI` / `EV` 幾乎必中任何英文標題。`_score_row()` 是 `score += 1.0 * len(themes)`，而整組技術面分數上限才 ~6.5 分。

實測：P&G 三則新聞「raises / retail / development」→ 判定題材 `['AI', 'EV/Battery']`、`score += 2.0`、欄位印「題材: AI, EV/Battery」。Exxon 一則「higher revenue」→ 命中 r-**EV**-enue。

改法：新增 `_kw_hit()`，2-4 碼全大寫的代號（AI/EV/GPT/LLM/GPU/AWS/LNG/CPI/OPEC/FOMC）改成「原始大小寫 + 整字」比對；其餘關鍵字維持寬鬆子字串比對，`chip` → `chips` 這類正常命中不受影響。

**驗證**（改後實跑）：

```
AI  in P&G  : False      AI  in 真 AI 新聞: True
EV  in P&G  : False      EV  in 真 EV 新聞: True
chip -> "chips" 仍命中: True   ← 沒有把對的改壞
```

---

## 🟠 A-8 5 個純台股掃描器把美股自選股也丟進去，每輪多打一堆必然失敗的請求

**位置**: `index_alerts.py:491 / 606`、`strong_stock_alert.py:162 / 350`、`short_candidates.py:130`

這 5 個 universe 全部只餵給台股專用的 metrics 函式（`_single_stock_weak_metrics` / `_stock_strength_metrics` / `_stock_weak_metrics`），三支都只試 `.TW` / `.TWO`。但無參數的 `load_watchlist_ids()` 依設計會回台美兩市。

自選股裡有 NVDA → 去抓 `NVDA.TW` / `NVDA.TWO`，必然空。修復前這 5 處是 TypeError 被吞掉（一檔都不加），現在變成「加了但全是廢呼叫」。同一輪的 `chip_anomaly` / `breakout_consolidation` / `analyst_insider` 都有正確傳市場參數，只有這 5 處漏了。改成 `load_watchlist_ids("TW")`。

---

## 🟡 A-9 「重大消息」把每季 10-Q、每年 10-K 也當 8-K 強制推播

**位置**: `news_event_alert.py:246-249`

`event_sources.fetch_finnhub_8k()` 實際放行五種 form（`8-K / 8-K/A / 10-Q / 10-K / S-1`），但呼叫端照函式名假設「回來的都是 8-K」，整批設 `_force_alert=True` → `hits = ["8K-AUTO"]` 直接跳過關鍵字過濾。

於是每季每檔美股自選股／持股的 10-Q、每年的 10-K 都會產生一則推播，而 `title` 只有 `"[10-Q] 2026-08-27 - NVDA"`（Finnhub filings API 沒有標題欄位）—— 你收到的是一則沒有任何資訊的「📰 重大消息」，還會佔掉 news_event 的每日額度與去重名額，排擠真正的 8-K。改成只有 `8-K / 8-K/A` 強制觸發，其餘照關鍵字走。

---

## 🟡 A-10 系統健康度的巢狀時間欄位查找方向錯了 →「盤中反轉」永遠顯示未啟動

**位置**: `system_health.py:116-127`

真實 state 的形狀是 `monitor_state["intraday_reversal"][symbol]["last_drawdown_at"]` —— **外層是 symbol、內層才是時間欄位**。但程式先做 `sub.get(tk)`，`sub` 的 key 是 `"^TWII"` 不是欄位名 → `v` 恆為 `None`，巢狀分支永遠進不去。9/1 那輪「補上缺的欄位名」補再多也找不到。

改成：平面查找先跑一輪，找不到才直接走 `sub.values()` 逐一鑽進 per-symbol 子 dict 取最大時間。

**驗證**（改後用真實 state 形狀重跑）：

```
intraday_reversal -> 2026-09-07 05:20:00   (原本恆為 None)
news_event_alert  -> 2026-09-07 01:08:00   (平面 state 不受影響)
```

⚠️ `weak_open` 仍會顯示「未啟動」，但那是另一回事：它 per-symbol 只存 `{date, weak_alerted, strong_alerted}`，**根本沒有任何時間欄位**（`index_alerts.py:1110`）。要修得在 `index_alerts.py:1225` 那邊補寫 `last_alert_at`，屬於行為改動，我沒動 —— 見 B-8。

---

## 🟡 A-11 Minervini：資料 210~220 根時第 3 條恆為 False；`pass_n=None` 會 TypeError

**位置**: `minervini_screener.py:40 / 55 / 123-131`

1. `ma_200_prev = c.rolling(200).mean().iloc[-22]` 需要至少 **221** 根，但門檻只擋 `< 210`。長度落在 210~220 時 `ma_200_prev = NaN`，`nan > nan` → `False` → 條件 3「200MA 一個月上升」被無條件吃掉，`pass_n` 最多只到 7/8，`pass_8_of_8` 永遠 False。yfinance `period="1y"` 遇到新上市股或資料缺漏就會落在這個區間。門檻改成 222。

2. 資料不足時程式明確寫入 `out["pass_n"] = None`，所以 `res.get("pass_n", 0)` 回的是 `None` 不是 `0` → `None >= min_pass` 直接 TypeError，`sort` 的 key 也一樣炸。改成先取出來判 `is None`。（`filter_minervini_picks` 目前全 repo 沒有 caller，是待爆的地雷而非現行故障。）

**驗證**：`len(df)=210/215` → `rolling(200).mean().iloc[-22]` = NaN；`len(df)=221` → 有值。`None >= 7` 實測 TypeError。

---

## 🟡 A-12 TG「設停損」按鈕會抹掉當日 fired 紀錄 → 同一天重複推停損警報

**位置**: `scripts/tg_callback_listener.py:166`

`holdings_tracker.check_stop_loss_breaches()` 的一日一次去重完全靠 `fired_date` / `near_stop_fired_date` 這兩個欄位，而同樣寫這個位置的 `update_stop_loss_state()` 有特地保留它們，還寫了註解「保留 fired 紀錄, 避免 Gemini 重設停損後同日再推一次」（`holdings_tracker.py:241-243`）。9/1 新加的 TG 按鈕路徑照抄了 key 位置、漏抄了這個保留邏輯。

情境：09:35 某檔跌破停損推播一次 → 你在 TG 按「設停損」重設 → `fired_date` 被抹掉 → 11:02 那班 cron 該檔仍在停損價下方 → **再推一次一模一樣的警報**。已補回這兩個欄位的保留。

---

# 第二段：找到了但我沒動，需要你決定（12 項）

這些要嘛是**產品取捨**（改了會大幅改變推薦名單），要嘛需要**真實 API 資料**才能確認，所以我只列出來。

## 🔴 B-1 `_run_with_timeout` 的逾時工作仍會拖住整個 job，monitor 的 state 有整批遺失風險

**位置**: `scripts/market_open_alert.py:93-118`、`1399-1402`

8/29 的 P0-2 讓主流程準時往下走了（這半邊有效），但註解說的「daemon thread 會隨 process 結束被回收」不成立：Python 3.9+ 的 `ThreadPoolExecutor` worker 是**非 daemon**，`threading._shutdown()` 會在 `atexit` callback **之前** join 所有 worker。

所以整個 process 仍會在結束時卡到 `fn()` 自然跑完，而 `atexit` 註冊的 `close_batched_state()`（monitor 模式下**所有** state 寫入都只在記憶體，靠它一次 flush）也跟著被延後。配上 workflow 的 `timeout-minutes: 20`，只要 yfinance 掛住（socket 沒 timeout 就是無限），job 會在 flush 之前被砍 → **這個 tick 的 `mark_alerts_sent`、去重、`push_cap` 計數、各模組冷卻全部丟失** → 下一個 tick 重複推。同一個誤解也在 `news_sources.fetch_external_signals_bounded()`（`news_sources.py:334-337`）。

實測（逐字複製的 helper，timeout=2s / 工作 8s）：

```
[timeout log] slow > 2s  t=2.03s
[main] continued t=2.03s        ← 主流程準時走了 ✅
[worker] finished  t=8.04s
[atexit] ran at    t=8.04s      ← flush 被延後 6 秒 ❌
real 0m8.071s
```

**建議改法**：把 executor 換成真正的 daemon thread + queue，並在 monitor 區塊尾端 `return 0` 之前加一次顯式 `_ws_batch.close_batched_state()`（atexit 那個留著當保險，它是 idempotent）。這動到排程主幹，我不敢在沒有你確認的情況下改。

---

## 🔴 B-2 全部技術指標吃未還原價，台股除權息當天憑空多一根假跌

**位置**: `data_sources.py:437`（`auto_adjust=False`，全 repo 從未使用 `Adj Close`）

台股現金殖利率普遍 3-6%，除息日未還原價會整段下移。合成一檔穩定小漲的股票在 t=120 除息 5%：

| | 還原後 | 未還原 |
|---|---|---|
| 當日漲跌 | -0.25% | **-5.24%** |
| RSI14 | 63.3 | **44.4**（差 18.9 點）|
| 站上 MA20 | True (122.63 > 120.19) | **False** (116.50 < 119.88) |

`entry_evaluator.py:390` 的 `tp <= -2.0 → score -= 5「❌ 今日大跌」` 在除息日必觸發；`fake_rally_detector` / `closing_analyzer` / `limit_up_precursor` 全部吃這根假跌幅。

**為什麼我不敢改**：`auto_adjust=True` 是一行的事，但它會同時改變**每一個**指標、每一條門檻、`backtest.py` 的歷史結果，還會讓「除權息缺口／填權息」這類刻意看原始價的判斷失效。這是產品層級的決定，不是 bug fix。建議做法：`auto_adjust=False` 保留，但攤平欄位後依 `Adj Close/Close` 比例還原 OHLC 給指標用，另存 `Raw Close` 給缺口偵測用。

---

## 🟠 B-3 台股均線/KD/MACD 觸發：偵測到了、警戒被吃掉了、但一封都不會送出

**位置**: `watchlist_triggers.py:107`、`scripts/market_open_alert.py:1604-1622`、`tw_post_market_summary.py:353`

cross 類觸發（`kd_*` / `macd_*` / `ma_cross_*`）走 `require_closed=True`，TW 要求 UTC hour ≥ 6。但台股 session 的 monitor cron 全在 UTC 01–05，永遠不滿足。真正評估到的第一個 tick 是 `45 13 * * 1-5`（TPE 21:45），寫進 queue 的 `date` 是 D。

可是消費端 `build_post_market_msg()` 在 `tw_close`（TPE 15:03）跑 —— 當天的 15:03 早就過了，隔天 15:03 的 `today_str` 是 D+1，於是 D 那筆被 `t.get("date") == today_str` 濾掉。同時 `mark_triggers_fired()` 已經把 `armed` 設成 False（one-shot 用掉）。

**結果**：你設的「跌破 20 日均線通知我」「KD 黃金交叉通知我」偵測到了、被消耗掉了、一封都不會送。（`price_above` / `price_below` 走 `require_closed=False`，正常。）

**建議改法**（`tw_post_market_summary.py:353` 附近，讓消費端接受昨天的 TPE 日期，因為 cross 觸發本來就是拿前一個收盤算的）：

```python
_now_tpe = (_dt.datetime.utcnow() + _dt.timedelta(hours=8)).date()
_valid = {_now_tpe.strftime("%Y-%m-%d"),
          (_now_tpe - _dt.timedelta(days=1)).strftime("%Y-%m-%d")}
wt_today = [t for t in wt_today if t.get("date") in _valid]
```

---

## 🟠 B-4 美股 cross 觸發：夏令會動、冬令整季不動

**位置**: `watchlist_triggers.py:100-107`

8/29 的 ⏸P2-4 採了「把門檻放寬到 UTC ≥ 20」，但只套在 DST 那一半，EST 那半仍是 21。而 monitor 的美股時段 cron 最晚是 UTC 20:15。冬令時 `20 < 21` → `_is_market_closed_now("US")` 回 False → 直接 `return None`。

**每年 11 月到 3 月，美股自選股的均線/KD/MACD 觸發完全不會被評估，而且是靜默的**。這種季節性行為差異比「一直不動」更難察覺。

兩個選項：(a) workflow 補一條 `15 21 * * 1-5` cron（冬令才是真收盤，比較正確）；(b) 冬令改用「已過 20:15」當近似收盤。要選哪個是你的取捨。

---

## 🟠 B-5 千張大戶的 regex 把 `1,000-5,000 股`（1~5 張的散戶級距）也算進去

**位置**: `chip_advanced.py:110`

```python
is_large = df[level_col].astype(str).str.contains("1000|1,000|>1000|≥1000", regex=True)
pct_now = float(large_df[large_df["date"] == last_date][pct_col].sum())
```

FinMind `HoldingSharesLevel` 的級距是以**股**為單位。真正的千張大戶只有 `more than 1,000,001`，但 `1,000` 這個 pattern 會同時命中 `1,000-5,000`（散戶裡人數最多的一檔）與 `800,001-1,000,000`，而且下面還 `.sum()` 全部加起來。

假設 `1,000-5,000 = 11.4%`、`800,001-1,000,000 = 2.1%`、`>1,000,001 = 38.6%` → 程式算出「千張大戶比例 **52.1%**」，正確值 **38.6%**。`change_pct` 因此混入散戶級距的變化，`trend` 判定失真，再進 `chip_advanced_score()` 的 `+10 / -5`，最後被 `upside_screener.py:687-691` 加到 top-10 的 score 上重排。

**為什麼我不敢改**：正確改法是「解析級距下界、取 ≥ 1,000,000 股」，但我沒有 FinMind token，無法確認你這個帳號實際拿到的級距字串長什麼樣（不同 dataset 有機會是「張」）。改錯會讓這個指標整個歸零。**建議你在有 token 的環境印一次 raw 的 `HoldingSharesLevel` 值，確認後我再改。**

```python
def _lower_bound_shares(s):
    nums = re.findall(r"[\d,]+", str(s))
    if not nums: return None
    try: return int(nums[0].replace(",", ""))
    except ValueError: return None
lb = df[level_col].map(_lower_bound_shares)
is_large = lb.notna() & (lb >= 1_000_000)
```

順帶：`_try_fetch(_LARGE_HOLDER_DATASETS, ...)` 會先回 `TaiwanStockShareholding`，該 dataset 沒有 `HoldingSharesLevel` / `percent` 欄位 → 第 107 行直接 `return {}`，所以這個指標實務上可能根本沒跑到。同一次確認可以一起看。

---

## 🟠 B-6 「動能繼續」沒有下界，跌 50% 的股票會顯示「還有 50% 上漲空間」

**位置**: `upside_screener.py:110 / 353-355 / 405`

`max_pct_from_52w_high: -8.0` 只有上界沒有下界，`upside_pct = abs(pct_hi)` 也沒有上限。一檔一年前從 300 崩到 100、近兩個月反彈到 150 的股票可以同時滿足全部 momentum 條件，然後：

- `pct_hi = -50.0` → 過關（`-50 <= -8`）
- `upside_pct = 50.0` → 推播印「**距 52w 高還有 50.0% 空間**」，分類寫「動能繼續」
- 同一檔的 ATR 短線目標只給 +4% —— 同一張卡片上兩個相差 12 倍的數字

美股版 `us_upside_screener._check_breakout` 有夾住（`pct_hi >= -10`、`upside` 上限 20），台股版沒有。

**建議改法**（會改變推薦名單，所以留給你決定）：

```python
"min_pct_from_52w_high": -30.0,   # 跌超過 30% 的不算「動能繼續」, 該走 reversal
...
upside_pct = min(abs(pct_hi), 25.0)
```

---

## 🟡 B-7 `20d漲跌%` 其實是「整個抓取視窗的漲跌」，`days=10` 時變成 6 日漲跌

**位置**: `chip_analyzer.py:214`

```python
price_summary["20d漲跌%"] = round((c.iloc[-1] / c.iloc[0] - 1) * 100, 2)   # iloc[0] 隨 days 浮動
price_summary["5d漲跌%"]  = round((c.iloc[-1] / c.iloc[-6] - 1) * 100, 2)  # 這行是對的
```

`days` 是**日曆日**：`upside_screener` 傳 30 → 約 20 根，剛好對；`chip_price_divergence` 傳 10、`avoid_signals` 傳 10 → 只有 6~8 根。所以同一檔股票同一天，兩個模組會印出兩個不同的「20d 漲跌%」，而 `chip_analyzer.py:436` 的推播字串直接寫 `20d {...:+.2f}%`。

連帶：`avoid_signals` 的「跌破月線」門檻 `-2%` 是照 20 日訂的，套在 7 日窗口上會系統性少觸發；同一份 chip 的「量比」在 `len(v) < 21` 時退回 6 日均量比。

兩條路：(a) `chip_analyzer.py:214` 固定 `c.iloc[-21]`，不足就回 None；(b) `avoid_signals.py:105` 與 `chip_price_divergence.py:76` 把 `days` 改成 30。**(a) 會讓 days=10 的呼叫端拿到 None，(b) 會多打 API** —— 選哪個是取捨，我沒動。

---

## 🟡 B-8 `weak_open`（開盤即弱）在健康度面板永遠是「未啟動」

**位置**: `index_alerts.py:1110 / 1225`

per-symbol state 只存 `{date, weak_alerted, strong_alerted}`，**沒有任何時間欄位**。A-10 修好了 `intraday_reversal`，但這支補再多欄位名候選也找不到。要嘛在 `index_alerts.py:1225` 那邊補寫 `last_alert_at`，要嘛把它從健康度表拿掉 —— 別讓它假裝「未啟動」。這是行為改動，留給你。

---

## 🟡 B-9 川普政策推播 claim 在送出前，送失敗沒有回滾 → 該則永久消失

**位置**: `scripts/market_open_alert.py:472-485`

同一輪修法在 `asia_leading` / `analyst_insider` / `breakout` / `news_event` / `vol_breakout` / `chip_anomaly` 六處都補上了「送失敗 → `unmark_alerts_sent()` 回滾」，唯獨 `trump_policy` 沒有 —— 而且 `trump_policy_alert.py` 根本**沒有定義 `unmark_alerts_sent`**。

`mark_alerts_sent()` 一次燒掉三樣：`sent_hashes`（200 筆滾動、永久去重）、`last_sent_ts`（360 分鐘冷卻）、`daily_count`（每日上限 3）。而這次還給它加了 `category="trump_policy"`，代表當天 6 封次要額度用完後（A-1 那條很容易燒光），每一則川普政策新聞都會被「標記已推 → 擋下 → 永遠不再重試」。

要修得先在 `trump_policy_alert.py` 補一支對稱的回滾函式，屬於新增功能而非修 bug，我沒動。

---

## 🟡 B-10 長訊息分段用「字元數」、上限判定用「UTF-16 單位」→ emoji 密集時可能無限遞迴

**位置**: `notifier.py:193-194` 與 `3473-3486`

`_u16_len` 對每個 astral 字元（大部分 emoji）算 2，`len()` 算 1。若一則訊息 `len(text) <= 3800` 但含 201 個以上 emoji，`_u16_len(text) > 4000` 成立而 `_split_tg_msg` 的 `while len(text) > max_chars` 一次都不會進去 → 回傳 `[text]`（跟輸入完全相同）→ 遞迴呼叫 `send_message` 又走同一條路 → `RecursionError`。症狀是「這封推播整封消失，log 只有一行 exception」。

3800 字元配 200+ emoji 在 `fmt_holdings_daily` / `fmt_tw_close_analysis` 這種每行 1-2 個 emoji 的訊息裡是搆得到的邊界。修法是讓分段器改用 `_u16_len` 當同一把尺並保證每輪一定有進展 —— 但 `_split_tg_msg` 是所有推播的共用路徑，我不想在沒實測的情況下動它。

---

## ⚪ B-11 投機股的 `atr_30d` 用收盤差絕對值，不是 True Range，波動率低估約 2 倍

**位置**: `speculation_screener.py:63-66`（門檻在 `116-117`）

只用了 `close.diff().abs()`，完全沒碰 `High` / `Low`，缺 True Range 的三項。投機股盤中振幅遠大於收盤價差，系統性低估。合成資料（日 sigma 3% + 盤中額外振幅）：專案算 `2.28%` → `<= 3.0` 成立 → **+5 分「✅ 波動收斂」**；真 ATR% = `4.91%` → 不該加分。

`indicators.atr_pct()` 已經有正確實作，換過去是三行的事 —— 但門檻 `3.0` 是照舊算法調出來的，換算法要一起重調門檻，否則這條加分項會直接失效。

---

## ⚪ B-12 `closing_analyzer` 的量縮判定 5 日與 20 日視窗重疊

**位置**: `closing_analyzer.py:259-262`

`tail(20)` 包含了 `tail(5)`，分母被近 5 日自己拉低。前 15 日均量 100、近 5 日均量 78（真實量縮到 0.78）：專案算 `78/87.5 = 0.825` → `< 0.80` 不成立 → 只拿 +1.0；正確 `78/100 = 0.780` → 應該拿 +2.0。`_score_breakout` 的入選門檻是 5.5，少 1 分足以把一檔真的在量縮整理的股票踢出「整理待噴」清單。

同一份 repo 的 `indicators.volume_dryup()` 用的是不重疊視窗，兩邊定義不一致。改法是 `vol.iloc[-20:-5]` —— 一樣會改變名單，留給你。

---

### 其他兩條（低優先，僅記錄）

- **`rsi_divergence.py:36-37 / 98-102`**：`ewm` 少了 `adjust=False`（跟 `indicators.rsi()` 不是同一個公式），而且先切 60 根才算 RSI → 視窗前段拿到暖機值。400 條隨機路徑實測，swing 常落點 idx 14-25 的誤差中位 **6.5 點**、最大 21.8 點，而 `MIN_RSI_DIFF` 只有 **2.0** → 假背離／漏背離都會發生。
- **`entry_evaluator.py:119-120 / 133-137`**：同樣缺 `adjust=False`（同一檔股票在 dashboard 上會有兩個 RSI 值）；`from_52w_high_pct` 其實只用了 `period="6mo"` 的最高價，一檔 9 個月前創高、之後跌 40% 的股票會被判成「距高點 -1%、強勢突破」+3 分。

---

# 第三段：查過，確認「不是 bug」（避免下一輪重複追）

- **8/29 那 15 項「✅ 已改」，14 項正確套用在正確位置**（唯一例外是 P2-1 → 見 A-2）。逐條複核過 P0-1 的 helper 與 8 個呼叫點、P0-2、P0-3 的 `fetch_shares_outstanding`、P0-4 的合約篩選與 `+=` 累加、P0-5 的 `_us_stock_to_tw_themes`、P0-6 的 `last_bucket`/`last_alert_at` 回寫、P1-1、P1-2、P1-4 的三個欄位名、P1-5 的兩個 key、P3-1、P3-2、P3-4、P3-5。
- **`.github/workflows/market_open_alert.yml` 的 25 條 cron 逐條對照 `case "$SCHED"` 分支，全部命中，沒有任何 slot 會落空。**
- **`indicators.py` 的 `atr()` / `rsi()` / `bollinger_bands()` / `rvol()` / `volume_expansion()` / `volume_dryup()` 公式都正確**（Wilder `adjust=False`、TR 三項齊全、量能視窗不重疊、RVOL 分母排除當日）。`tw_screener._kd_series()` 與 `_macd_series()` 也是標準寫法。
- **`backtest.py` 沒有未來函數**：`_run_one_condition` 的 slice 是 `date <= as_of`，`_forward_returns` 只取 `date > as_of`。
- `push_dedup.reset()` 對已持久化的 claim 是 no-op —— 檔案裡已寫明並提供 `force_claim()`，呼叫端也確實用了，是刻意設計。
- `index_alerts` 的 bucket 警報已被你關掉，`check_index_alerts()` 現在是死碼 —— 刻意的，不是 bug。
- `_workflow_updates/*.yml` 是舊版快照（單一併發群組、整點 cron、`pre_market.yml` 末尾還有一行殘留的 `python scripts/your_pre_market_script.py`），不在任何 workflow 的路徑上。**建議刪掉，避免下次誤把它當成現況。**
- `short_interest_alert._fetch_margin_short` 的 `today[0]` 只取單列 —— 若 FinMind `TaiwanStockTotalMarginPurchaseShortSale` 是 long-format 就會抓錯類別，但離線無法確認，留給你在有 token 時印一次 raw rows。

---

# 最後：還是那件事

`stock_dashboard` 資料夾**到現在還是沒有 `.git`**。這次改的 12 項，跟 8/24、8/29 改的一樣，**GitHub 上跑的那份完全拿不到**。上面 A-1（重複推播燒光額度）、A-2（大盤警報誤報）、A-3（自選股沒進盤後分析）都是每天在發生的，但除非部署上去，否則改了等於沒改。

原始檔已備份在 `_backup_2026-09-07/`，要回退任何一項直接覆蓋回去即可。
