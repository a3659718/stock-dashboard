# 推播稽核 + 隔日驗收功能 (2026-08-25)

## 〇、最重要的一件事:這些修改還沒生效

`stock_dashboard` 資料夾裡**還是沒有 `.git`**(跟 8/24 那份診斷一樣)。
我改的是你本機的檔案,**GitHub Actions 上跑的還是舊版**。要生效必須先把本機接回 repo 再 push。

---

## 一、你要的「隔日驗收」— 做好了

從**明天早上 08:00 晨報**開始,最上面會多這一段:

```
🎯 昨日推薦驗收 — 隔一個交易日收盤 vs 推薦當下價
🇹🇼 台股推薦驗收 (8/24 收盤結算)
  ✅ 2330 台積電 +1.98%
  ✅ 6488 環球晶 +1.94%
  ❌ 2454 聯發科 -1.98%
  🟢 隔日上漲比例 2/3 = 67% · 平均 +0.65%

🇺🇸 美股推薦驗收 (8/24 收盤結算)
  ✅ NVDA Nvidia +5.88%
  ❌ AMD AMD -4.04%
  🟡 隔日上漲比例 1/2 = 50% · 平均 +0.92%

  📊 近 30 日累計:🟢 38/60 = 63% · 平均 +0.71%
  另有 4 檔等待結算
```

### 判定標準(依你選的)

**隔一個「交易日」的收盤價 > 推薦當下的價格 → 算有漲**,門檻 0%,不扣手續費滑價。

### 納入統計的推播

| 推播 | 時間 | 記錄的訊號 |
|------|------|-----------|
| 晨報 台股推薦 Top 5 | 08:00 | `morning_tw_top5` |
| 台股盤前 可買 Top 5 | 08:30 | `pre_market_buy` |
| 開盤選股 強勢族群龍頭 | 09:32 | `strong_sector_leader` |
| 盤後 隔日上漲 Top 3 | 15:03 | `next_day_breakout` |
| 美股盤前 BUY Top 5 | 20:32 / 21:32 | `us_buy_picks` |

> 你在選單裡只勾了「開盤選股」,但原訊息寫「台股美股都是」— 兩者矛盾,
> 我照原訊息做成**全部都納入**(只勾開盤選股的話美股就完全沒東西可驗)。
> 要縮回去很簡單:把不想要的那幾支 `pick_review.record_picks(...)` 註解掉即可。

### 為什麼分組是用「結算日」而不是「推播日」

不同推播的價格基準不一樣:晨報 08:00 引用的是**前一交易日收盤**(隔天 = 當天盤),
09:32 開盤選股引用的是**當天盤中價**(隔天 = 明天盤)。若照推播日分組,同一天推的兩批
結算日會差一天,晚結算的那批永遠等不到被報導。改用結算日分組後**每一檔都會在結果出爐
後的第一封晨報被報一次,不重不漏**。

### 新增/改動的檔案

- **`pick_review.py`(新)** — 記錄推薦 + 產生驗收段落
- **`signal_tracker.py`(大改)** — 驗證邏輯從「曆天 + 抓最新收盤」改成「交易日 bar」
- **`scripts/test_pick_review.py`(新)** — 離線回歸測試,不打網路不用 secrets:
  ```
  python scripts/test_pick_review.py    # exit 0 = 12 項斷言全過
  ```

---

## 二、順手修掉的 bug(依嚴重度)

### 🔴 1. 每週五推的股票,一律被記成「沒漲」

**`signal_tracker.py`** — 舊的驗證邏輯是「`predicted_at` + N 個**曆天**到了,就抓該檔**最新**
一根日 K 的收盤來比」。週五推 → `evaluate_at` = **週六** → 週六抓到的「最新收盤」就是
週五自己 → 漲幅恆等於 0 → `0 > 0.5` 為 False → **每週五推的每一檔都被判定失敗**。
春節那種連假錯得更誇張。長期下來滾動勝率被系統性壓低,而且完全看不出來。

修法:改抓日 K 序列,以「推薦價所屬的那根 bar」為基準往後數**真的有交易的 bar**。
週末 / 國定假日 / 補班日全部自動正確,不用維護行事曆。

### 🔴 2. 美股的推薦從來沒被驗證過

同檔。舊的 `_is_market_closed_now("US")` 要求 UTC ≥ 21:00 才肯評估,但
`evaluate_pending()` 的兩個呼叫點(heartbeat 06:32 UTC / 台股盤後 07:03 UTC)
都不滿足 → 美股標的一律回 `None` → **永遠 pending,美股命中率永遠是空的**。

### 🔴 3. 持倉盤中風險警報(系統最高優先 Tier 0)算出來但從來沒送出

**`scripts/market_open_alert.py:1563`** — `holdings_intraday_alerts` 這個變數在整個檔案裡
只出現在「賦值 + print」。唯一能送它的
`notifier.fmt_combined_intraday_super(holdings_intraday_alerts=...)` 在**上面一百多行**、
而且根本沒傳這個參數。

結果:**持股當日跌超過 3%、從當日高點回吐 5%、跌破停損 —— 一封都收不到。**
`alert_priority` 裡這是 Tier 0(致命/立即動作)等級。

修法:偵測到就獨立送一封(Tier 0 本來就該獨立通知音,不該跟大盤警報混在一起),
**送成功才** `mark_alerts_sent`,送失敗下個 tick 會重試。

### 🔴 4. 每週五的美股收盤總結從來沒推過

**`holiday_check.py`** — `is_market_closed_today()` 對**所有市場**都用台北日期判斷。
美股排程落在 UTC 14:00–23:59,加 8 小時會**跨到台北的隔天**:

| slot | cron (UTC) | 週五觸發 | 台北日期 | 結果 |
|---|---|---|---|---|
| `us_close` (夏令) | `2 22 * * 1-5` | 週五 22:02 | **週六** | 判「週末休市」→ skip |
| `us_close` (冬令) | `2 23 * * 1-5` | 週五 23:02 | **週六** | skip |
| `us_mid` (冬令) | `32 16 * * 1-5` | 週五 16:32 | **週六** | skip |

假日則是整組差一天:感恩節**前一天**(有交易)被 skip,感恩節**當天**(休市)反而推了
一封「美股收盤分析」。

修法:加 `_today_et()`,US 用美東日期,TW/JP/KR 維持台北日期。

### 🔴 5. 「今日有新股上市」提醒是死碼

**`scripts/market_open_alert.py`** — IPO 今日上市的檢查寫在 `if market == "pre_market_830":`,
但 pre_market 區塊結尾有一行**無條件 `return 0`**,比它早 270 行 → 永遠到不了。
已搬到 `return` 之前。(週五的「下週 IPO 預告」是可達的,只有「今日上市」壞掉。)

### 🟡 6. 美股盤整突破警報每天早上被原封不動重推一次

**`breakout_consolidation_alert.py`** — 當日去重表用 `dt.date.today()`(UTC 日期),
但 UTC 日界落在**台北 08:00**,也就是亞股時段的 monitor tick(09:08~13:08)已經算新的一天:

```
週二 22:15 推 NVDA 突破 → alerted 記在 UTC 週二
週三 09:08 → UTC 翻頁 → 去重表清空
           → 美股已收盤, 日線最後一根還是週二那根, 條件原封不動全部成立
           → 同一批標的再推一次
```

改用美東日期。

### 🟡 7. 台股盤前 BUY 的「分數 ≥70」門檻形同虛設

**`pre_market_alert.py`** — 篩選條件寫 `entry_score is None or float(...) >= 70`,
「系統根本評不出分數」的股票直接**通過**高品質門檻而不是被排除。標題寫著
「entry_label=BUY + 分數 >=70」,實際可能推出沒有分數的標的。
美股版早就修掉了,台股版漏改。已對齊。

---

## 三、還沒動、但你該知道的問題

這幾項要嘛改動風險較高、要嘛是取捨問題,**我沒有動**,列出來給你決定:

### 🟡 A. Google Sheets 一掛,「每日 6 封上限」就退化成「每個 run 6 封」

`push_cap` / `push_dedup` / `alert_priority` 的狀態全靠 `load_monitor_state()`。
`monitor_state.json` 在 `.gitignore` 裡 → GitHub Actions checkout 後本地檔不存在 →
一旦 GSheet 掛掉(憑證過期 / quota / 429),每個 run 都拿到空 state,**每日上限、去重、
冷卻全部同時失效**。平日約 25 個 run,「每日 6 封」的實際上限會變成 ~150 封。

警告有印在 Actions log,但**不會推到 Telegram** → 你感受到的會是「突然一天收到幾十封」。
建議:GSheet 讀失敗時直接推一封「⚠️ 狀態儲存失效」給你自己。

> ⚠️ 這一項也直接影響新做的驗收功能 —— 推薦帳本存在同一個地方。GSheet 掛掉時
> 帳本每個 run 都會重置,驗收段落會長期顯示「尚無已結算的推薦」。
> 三個 workflow 的 `GCP_SERVICE_ACCOUNT_JSON` / `GOOGLE_SHEETS_ID` 都有接,
> **請確認這兩個 secret 還有效**(服務帳號金鑰有可能已過期)。

### 🟡 B. 反轉警報「送出前就標記已推」,送失敗會靜默吞掉

`scripts/market_open_alert.py` 在 send 前 117 行就 `mark_picks_pushed(reversal_alerts, ...)`,
中間任一步失敗只印 log → 這批警報被標成已推,30 分鐘內不再重試。
同檔的 `volume_breakout` / `chip_anomaly` 都有正確的 `unmark` 回滾,**只有 reversal 沒有**。

### 🟡 C. `alert_priority` 去重 key 不含 alert_type

key 是 `f"{stock_id}|{direction}"`,docstring 說會分 alert_type 但實際沒有。
後果:同一檔同方向,一則 Tier 3 的參考訊號可以壓掉 30 分鐘內的 Tier 1 反轉警報。

### 🟡 D. 早上 31 分鐘內 3 封,內容仍高度重疊

08:02 晨報 / 08:17 盤前 / 08:33 盤前 —— 晨報的「美股隔夜」跟盤前的美股快照本質是
同一批數據講兩三次。08:17 跟 08:33 只差 16 分鐘,日韓才開盤 17 分鐘,delta 極小。
(之前已經從 4 封減到 3 封,還可以再減一封。)

### 🟢 E. `morning_recap` 的 cron 還在,但 handler 已是 no-op

`market_open_alert.yml:85` 的 `32 23 * * 0-5`,每月約 26 次完整跑 checkout +
pip install + 一堆連線測試,最後只印一行就結束。建議直接刪掉這條 cron 和對應的 router case。

### 🟢 F. `us_buy_picks` 保證每天 2 封

沒有符合條件的標的時仍會送一封「今日無高品質 BUY」,加上同 slot 的走勢預測 = 保底 2 封。
跟 `heartbeat`(全健康不推)、`tw_mid`(±0.5% 才推)的「有內容才推」原則不一致。

### 🟢 G. cron 註解與實際時間對不上(多處)

`yml` 寫「14:30」實際是 14:32;訊息 label 寫死「開盤後 30 分鐘 (09:30)」實際 09:32;
`pre_market.yml` 寫 08:15/08:30 實際 08:17/08:33。使用者收到的時間戳是對的
(`notifier` 出口統一蓋台北時間),但會誤導維護者。

---

## 四、一天推幾封(夏令時 / 平日)

| 台北時間 | slot | 封數 |
|---|---|---|
| 08:02 | 晨報(**新增昨日驗收段**) | 1 必推 |
| 08:17 / 08:33 | 台股盤前 ×2 | 0–2 |
| 09:32 | 台股開盤 + 停損 + ETF 變動 | 最多 3 |
| 11:02 | 台股中盤 | 0–1(±0.5% 才推) |
| 14:32 | heartbeat | 0–1(全健康不推) |
| 15:03 | 台股盤後 + 籌碼背離 + 持倉日報 | 最多 3 |
| 16:32 | 外資出貨嫌疑 | 0–1 |
| 19:02 / 20:32 | 美股廣掃 / 美股盤前 BUY | 1 + 2 |
| 22:02 / 23:32 | 美股開盤 / 中盤 | 各 1+ |
| 06:02(次日) | 美股收盤 | 1(**修好後週五也會推了**) |
| 盤中 monitor | 反轉 / 大跌 / 強勢股 / 量爆 / 籌碼 | 0–n |

**保底約 8 封/日,尖峰 12–18 封/日。** 冬令時美股段整體 +1 小時
(`us_mid` 落在台北 00:32、`us_close` 落在 07:02)。

順帶一提:靜音規則只蓋台北 02:00–06:00,所以冬令時的 00:32 美股中盤、
夏令時 00:15 / 01:15 的 monitor 警報**會響鈴**。

---

## 五、建議的下一步

1. **先把本機接回 GitHub**(不然以上全部不生效)
2. 確認 `GCP_SERVICE_ACCOUNT_JSON` / `GOOGLE_SHEETS_ID` 兩個 secret 還有效
3. `python scripts/test_pick_review.py` 應該 exit 0
4. 手動觸發一次晨報,確認驗收段落有出現(第一天會顯示「尚無已結算的推薦」,
   要等推播記了一輪、隔一個交易日收盤後才有數字)
5. 上面第三節 A–G 想修哪幾項再告訴我
